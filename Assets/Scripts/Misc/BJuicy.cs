using UnityEngine;
using System.Collections;

public class BJuicy : MonoBehaviour
{
    void Update()
    {
        //Vector3 curPos = transform.position;
        //curPos.y = Mathf.Sin((curPos.x * 0.2f)+ Time.time) * 5.0f;
        //curPos.y += Mathf.Cos((curPos.z * 0.2f) + Time.time) * 5.0f;
        //transform.position = curPos;
    }
}
